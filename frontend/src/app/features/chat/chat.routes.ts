import { Routes } from '@angular/router';

// Chat is a panel overlay inside the shell, not a routed page.
// This file exists for future expandability (e.g., full-page chat view).
export const CHAT_ROUTES: Routes = [];
